export enum AccessConditions {
	whitelist = "WHITELIST",
	token = "ASSET_OWNER",
	stakePool = "POOL_DELEGATION",
}

export enum SortDirection {
	Ascending = "asc",
	Descending = "desc",
	None = "none",
}
